services:
  mysql:
    image: mysql:8.0
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: idempotency_gateway
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
